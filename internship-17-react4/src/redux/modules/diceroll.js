
// action types

// initial state

const initialState = {
  firstDice: 0,
  secondDice: 0
};

// action creators

// reducer
const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return { ...state };
  }
};

export default reducer;
